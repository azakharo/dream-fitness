/**
 * Date format used for displaying and parsing dates in the UI.
 * Used by DatePicker component and auth schema validation.
 */
export const DATE_FORMAT = 'dd.MM.yyyy';

/**
 * Time format used for displaying time in the UI.
 */
export const TIME_FORMAT = 'HH:mm';

/**
 * Combined datetime format for displaying date and time together.
 */
export const DATETIME_FORMAT = 'dd.MM.yyyy HH:mm';
