export enum UserGender {
  MALE = 'male',
  FEMALE = 'female',
}

export enum UserRole {
  CLIENT = 'client',
  ADMIN = 'admin',
}

export enum UserStatus {
  ACTIVE = 'active',
  BLOCKED = 'blocked',
}

export enum TransactionType {
  DEPOSIT = 'deposit',
  WITHDRAW = 'withdraw',
  REFUND = 'refund',
  RESERVE = 'reserve',
  RELEASE = 'release',
}
